<div class="card" style="width: 18rem;">
  <div class="card-header">
    <h5>Editar Perfil<% resource_name.to_s.humanize %></h5>
  </div>
  </div>
 <div class="card-body"> 

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field form-group">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control", placeholder:"Agregar Email", required:"required", type:"email" %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field form-group">
      <%= f.label :password %> <i>dejar en blanco, si no desea cambiarla</i><br />
      <%= f.password_field :password, autocomplete: "new-password", class:"form-control", placeholder:"Nueva Contraseña", required:"required", type:"email" %>
      <% if @minimum_password_length %>
        <em><%= @minimum_password_length %> characters minimum</em>
      <% end %>
    </div>

    <div class="field form-group">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control", placeholder:"Confirmar Contraseña", required:"required", type:"password" %>
    </div>

    <div class="field form-group">
      <%= f.label :current_password %> <br />
      <%= f.password_field :current_password, autocomplete: "current-password", class:"form-control", placeholder:"Se requiere contraseña actual, para confirmar los cambios", required:"required", type:"password" %>
    </div>

    <div class="actions form-group">
      <%= f.submit "Actualizar", class:"btn btn-primary" %>
    </div>
  <% end %>
    </div>
</div>


<div class="card">
  <div class="card-header">
    <h3>Eliminar mi Cuenta</h3>
  </div>
  <div class="card-body">
    <h5 class="card-title"></h5>
    <p class="card-text"><%= button_to "Eliminar mi Cuenta", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Está Seguro?" }, method: :delete, class:"btn btn-danger" %>      
    </p>
  </div>
</div>

<%= link_to "Volver", :back, class:"btn btn-dark" %>
