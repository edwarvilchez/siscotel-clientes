<p style="color: green"><%= notice %></p>

<h1>Clientes</h1>

<div id="clientes">
  <% @clientes.each do |cliente| %>
    <%= render cliente %>
    <p>
      <%= link_to "Show Client", cliente %>
    </p>
  <% end %>
</div>

<%= link_to "Add New Client", new_cliente_path %>
